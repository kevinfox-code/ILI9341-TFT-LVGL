cmake_minimum_required(VERSION 3.22)

#
# Top-level glue: never regenerated by CubeMX. CubeMX output lives entirely
# under Profiles/${PROFILE}/ (selected below, default board_1); everything
# else here wires App/, Profiles/${PROFILE}/, and the tft_drivers library
# together.
#

set(CMAKE_C_STANDARD 11)
set(CMAKE_C_STANDARD_REQUIRED ON)
set(CMAKE_C_EXTENSIONS ON)

if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE "Debug")
endif()

set(CMAKE_PROJECT_NAME 01_Display)
set(CMAKE_EXPORT_COMPILE_COMMANDS TRUE)

project(${CMAKE_PROJECT_NAME})
message("Build type: " ${CMAKE_BUILD_TYPE})

# Which Profiles/<name> to build against. Adding a new board is a
# one-folder (Profiles/board_2/) + one-cache-var affair — no other file in
# this list needs to change. Also read by Profiles/board_1/cmake/*.cmake
# (the toolchain file) to select the linker script.
if(NOT DEFINED PROFILE)
    set(PROFILE "board_1" CACHE STRING "Profiles/<PROFILE> to build against")
endif()

enable_language(C ASM)

add_executable(${CMAKE_PROJECT_NAME})

# STM32CubeMX generated sources for the selected profile.
add_subdirectory(Profiles/${PROFILE}/cmake/stm32cubemx stm32cubemx)

# App/ — tft_drivers, LVGL, and the application itself.
add_subdirectory(App)

# Hand-added CMSIS-RTOS2 shim not part of CubeMX's generated file list (see
# Profiles/${PROFILE}/Core/Src/cmsis_os2_ext.c for why osThreadDetach needs
# a stub on this FreeRTOS/CMSIS-RTOS2 port).
target_sources(${CMAKE_PROJECT_NAME} PRIVATE
    Profiles/${PROFILE}/Core/Src/cmsis_os2_ext.c
)

# Remove wrong libob.a library dependency when using cpp files
list(REMOVE_ITEM CMAKE_C_IMPLICIT_LINK_LIBRARIES ob)

target_link_libraries(${CMAKE_PROJECT_NAME}
    stm32cubemx
    app
)
